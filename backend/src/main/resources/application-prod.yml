spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/propease
    username: postgres
    password: iamSB@293
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
